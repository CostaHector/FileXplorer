#ifndef VERMENUINHEADER_H
#define VERMENUINHEADER_H

#include "MenuInHeader.h"

class VerMenuInHeader : public MenuInHeader {
public:
  explicit VerMenuInHeader(const QString& proName, QWidget* parent = nullptr);
private:
};

#endif // VERMENUINHEADER_H
